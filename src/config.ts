/* ══════════════════════════════════════════════════════════
   참호 사수 — 1인칭 시점을 흉내낸 2D 웨이브 디펜스
   좌표계: 논리 해상도 1280x720 고정, 화면에 맞춰 letterbox 스케일
   원근: 거리 d 를 1/(d+K) 로 투영 → y, 크기, 좌우폭이 같은 비율로 커진다.
         K 가 있어서 근거리에서 과하게 부풀지 않는다.
   ══════════════════════════════════════════════════════════ */

export const W = 1280, H = 720;
export const HORIZON = 290;          // 지평선 y
export const PROJ_K  = 0.6;          // 원근 완충
export const PROJ_Y  = 432;          // 발 위치 = HORIZON + PROJ_Y*p
export const PROJ_X  = 436;          // 화면x = 640 + wx*PROJ_X*p
export const PROJ_S  = 4.0;          // 스케일 = PROJ_S*p
export const D_SPAWN = 7.2;          // 스폰 거리 — 가깝게 붙여서 에임게임이 되지 않게
export const D_MELEE = 1.0;          // 이 거리에서 멈춰 참호를 때리기 시작한다
/* 동시 생존 좀비 상한. 웨이브 총량과는 별개다.
   250 으로 묶었을 때는 관통 빌드가 250마리를 영원히 앞질러서 후반에 참호가
   아예 긁히지도 않았다(자동플레이 44웨이브 내내 내구도 만피). 압박은 결국
   "한 번에 몇 마리가 밀려오는가"에서 나오므로, 렌더 비용을 줄여 확보한
   여유만큼 올린다. 실측 2.77ms/212마리 → 550마리에서도 8ms 안쪽. */
export const MAX_LIVE = 550;
export const FUNNEL  = 1.85;         // 좀비가 지나올 수 있는 통로 반폭(wx 단위)
                              // 양옆은 철조망으로 막혀 있어 전부 여기로 몰린다
